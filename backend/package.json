{
  "name": "backend",
  "module": "index.ts",
  "type": "module",
  "private": true,
  "scripts": {
    "clean": "rm -rf dist && echo 'Done.'",
    "dev": "bun --watch src/server.ts",
    "run:db": "bun src/db/db.ts",
    "start": "bun run src/server.ts",
    "docs": "typedoc"
  },
  "devDependencies": {
    "@types/bun": "latest",
    "typedoc": "^0.28.5"
  },
  "peerDependencies": {
    "typescript": "^5"
  },
  "dependencies": {
    "dotenv": "^16.5.0",
    "effect": "^3.16.8",
    "hono": "^4.7.10"
  }
}