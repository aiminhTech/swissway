{
  "name": "backend",
  "module": "index.ts",
  "type": "module",
  "private": true,
  "scripts": {
    "clean": "rm -rf dist && echo 'Done.'",
    "dev": "bun --watch src/api.ts",
    "run:db": "bun src/db/db.ts",
    "docs": "typedoc"
  },
  "devDependencies": {
    "@types/bun": "latest",
    "typedoc": "^0.28.5"
  },
  "peerDependencies": {
    "typescript": "^5"
  },
  "dependencies": {
    "hono": "^4.7.10"
  }
}